using UnityEngine;

public class SatelliteTravel : MonoBehaviour
{
    public Transform origin;      // ponto de partida (Terra)
    public Transform destination; // ponto de chegada (Netuno)
    public float travelDuration = 10f; // quantos segundos a viagem vai durar

    private float elapsedTime = 0f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}